{
  "name": "social_media",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install": "node scripts/nested-json.js",
    "start": "concurrently --kill-others \" cd ./client && npm run watch\" \" cd ./server && npm run watch\"",
    "clearModules": "npm prune"
  },
  "devDependencies": {
    "concurrently": "^7.2.2",
    "prettier": "^2.7.1"
  }
}
